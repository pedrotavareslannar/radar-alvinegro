Uppercase, gold, wide-tracked category label that sits directly above a headline.

```jsx
<Kicker>Transferências</Kicker>
```

Pass `as="div"` to change the element. Pairs with NewsCard and article headers.
